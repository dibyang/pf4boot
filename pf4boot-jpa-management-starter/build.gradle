dependencies {
    implementation project(":pf4boot-api")
    implementation project(":pf4boot-jpa")
    implementation project(":pf4boot-management-starter")
    implementation("net.xdob.springframework.boot:spring-boot-actuator:${spring_boot_version}") {
        exclude group: 'org.slf4j'
    }
    compileOnly("net.xdob.springframework.boot:spring-boot-starter-web:${spring_boot_version}") {
        exclude group: 'org.slf4j'
    }
    compileOnly("net.xdob.springframework.boot:spring-boot-autoconfigure:${spring_boot_version}") {
        exclude group: 'org.slf4j'
    }
    compileOnly 'javax.servlet:javax.servlet-api:3.1.0'
    testImplementation("net.xdob.springframework:spring-test:$spring_version")
    testImplementation 'javax.servlet:javax.servlet-api:3.1.0'
}
